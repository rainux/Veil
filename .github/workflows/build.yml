name: Build

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Install neovim
        run: brew install neovim

      - name: Build
        run: xcodebuild build -project Veil.xcodeproj -scheme Veil -destination 'platform=macOS' -configuration Release CODE_SIGNING_ALLOWED=NO

      - name: Test
        run: xcodebuild test -project Veil.xcodeproj -scheme Veil -destination 'platform=macOS' -only-testing:VeilTests CODE_SIGNING_ALLOWED=NO

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: macos-15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Build Release
        run: xcodebuild build -project Veil.xcodeproj -scheme Veil -destination 'platform=macOS' -configuration Release -derivedDataPath build CODE_SIGNING_ALLOWED=NO

      - name: Package app
        run: |
          APP_PATH="build/Build/Products/Release/Veil.app"
          ditto -c -k --keepParent "$APP_PATH" Veil.zip

      - name: Extract tag annotation
        id: tag_msg
        run: |
          TAG_MSG=$(git tag -l --format='%(contents)' ${GITHUB_REF_NAME})
          echo "body<<EOF" >> $GITHUB_OUTPUT
          echo "$TAG_MSG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: Veil.zip
          body: ${{ steps.tag_msg.outputs.body }}
